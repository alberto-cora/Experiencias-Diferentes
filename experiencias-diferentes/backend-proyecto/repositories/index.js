const usersRepo = require('./users-repo');
const activitiesRepo = require('./activities-repo');
const booksRepo = require('./books-repo');
const ratingsRepo = require('./ratings-repo');

module.exports = {
    usersRepo,
    activitiesRepo,
    booksRepo,
    ratingsRepo,
};
